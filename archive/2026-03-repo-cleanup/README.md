# 2026-03 Repo Cleanup

These directories were moved out of the repository root on 2026-03-29 to make the active app structure unambiguous.

Nothing here is used by the current runtime.

Contents:

- `home/`: root-level static prototype pages
- `legacy/`: earlier archived prototype assets
- `Progression tracker app/`: nested duplicate of a later app snapshot
- `Progression_Tracker_App/`: older application version from before the current `src/` + `public/` structure
