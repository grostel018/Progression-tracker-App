# Legacy Files

This folder contains archived files that are no longer part of the active app structure.

- `home-static-prototype/`: the older standalone HTML/PHP prototype
- `public-legacy-assets/`: the old root-level `public/style.css` and `public/script.js`

The active application lives in `src/` and `public/`.
